function add(a, b) {
  return `parseInt(${parseInt(a)} + ${parseInt(b)})`;
}

function subtract(a, b) {
  return `${a} - ${b}`;
}

function multiply(a, b) {
  return `${a} * ${b}`;
}

function divide(a, b) {
  return `${a} / ${b}`;
}